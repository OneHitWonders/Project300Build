using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerCollect : MonoBehaviour
{
    public int ammo = 0;
    public int medical = 0;
    public int construction = 0;
    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    //private void OnGUI()
    //{
    //    GUI.Label(new Rect(30, 100, 200, 40), "Ammo supplies : " + ammo);
    //    GUI.Label(new Rect(30, 160, 200, 40), "Medical supplies : " + medical);
    //    GUI.Label(new Rect(30, 220, 200, 40), "Construction supplies : " + construction);

    //}
}